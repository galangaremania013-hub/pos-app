<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WorkID" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:statusBarColor">@color/dark_slate</item>
        <item name="android:navigationBarColor">@color/dark_slate</item>
        <item name="android:windowBackground">@color/background_primary</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.WorkID.Splash" parent="Theme.WorkID">
        <item name="android:windowBackground">@color/background_primary</item>
    </style>
</resources>
